<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grupo2.devmob.projetodevmobgrupo2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/imglogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MesasActivity" />
        <activity android:name=".CardapioActivity" />
        <activity android:name=".PedidoActivity" />
        <activity
            android:name=".SelectCardapio"
            android:label="@string/title_activity_select_cardapio"></activity>
    </application>

</manifest>